@*
    Rmove Router as its not needed for MVC
*@
@*<Router AppAssembly="typeof(Startup).Assembly" />*@

@ChildContent

@functions {
    [Parameter]
    private RenderFragment ChildContent { get; set; }
}